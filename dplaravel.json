{
  "name": "test",
  "branch": "master",
  "dest": "build",
  "commands": [
    { "cmd": "gulp --production" },
    { "cmd": "composer install" }
  ],
  "sftp": {
    "host": "",
    "user": "user",
    "password": "password",
    "directory": "/var/www"
  }
}
