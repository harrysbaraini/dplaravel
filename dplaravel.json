
{
  "name": "project-name",
  "branch": "master",
  "dest": "build",
  "commands": [
    { "cmd": "composer install --no-ansi --no-dev --no-interaction --no-scripts --optimize-autoloader" },
    { "cmd": "gulp --production" }
  ],
  "sftp": {
    "host": "",
    "user": "user",
    "password": "password",
    "directory": "/var/www"
  }
}
